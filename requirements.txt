# ============================================================
# Anonimizzatore PDF - Dipendenze Python v1.1.2
# Python 3.12 richiesto
# ============================================================
#
# Versioni MINIME esplicite per chiudere vulnerabilità note:
# - Pillow >= 10.2.0  → fix CVE-2023-50447 (ImageMath.eval RCE)
# - streamlit >= 1.37.0 → fix CVE-2024-42474 (path traversal Windows)
#
# Bound massime per evitare breaking changes (major version bump):
# - spacy < 3.8 (compatibilità con it_core_news_lg)
# - Pillow < 12 / streamlit < 2 (semver protection)
#
# Per ambienti production usare requirements-lock.txt
# (generato con: pip-compile --generate-hashes requirements.txt)
# ============================================================

# UI Web
streamlit>=1.37.0,<2.0.0

# Manipolazione PDF
pymupdf>=1.24.0,<2.0.0

# NLP e riconoscimento entità
# NOTA: presidio-anonymizer NON serve (la redazione la fa PyMuPDF)
presidio-analyzer>=2.2.0,<3.0.0
spacy>=3.7.0,<3.8.0

# OCR per PDF scansionati
pytesseract>=0.3.10,<1.0.0
Pillow>=10.2.0,<12.0.0

# NOTA: il modello linguistico italiano va installato separatamente con:
# python -m spacy download it_core_news_lg
